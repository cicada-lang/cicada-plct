{
  "name": "@cicada-lang/cicada-next",
  "version": "0.2.1",
  "repository": "github:cicada-lang/cicada-next",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "test": "vitest --run --dir src",
    "format": "npx prettier src --write"
  },
  "dependencies": {
    "@cicada-lang/partech": "^0.1.20"
  },
  "devDependencies": {
    "@types/node": "^18.7.13",
    "prettier": "^2.7.1",
    "prettier-plugin-organize-imports": "^3.1.0",
    "typescript": "^4.8.2",
    "vitest": "^0.22.1"
  },
  "license": "GPL-3.0-or-later"
}
